import type { CreateSystemShortcuts } from '$lib/shortcuts/types.js';

/** The shared shortcut router keeps browser shortcuts inside the focused page. */
export const createSystemShortcuts: CreateSystemShortcuts | null = null;
